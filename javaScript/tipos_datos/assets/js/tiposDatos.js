let nombre = "Juan"; // es un string
let edad = 30 // es un number

const gravedad = 9.81 //const no se puede reasignar un valor

let dinero = 23.3  //es un número flotante
let tinesTrabajo = true; // es un boolean
let variableNull = null; //es una variable con tipo de dato nula

let varaiableIndefinida; // no se define con ningun valor

console.log(typeof(varaiableIndefinida));
console.log(typeof(nombre));
console.log(typeof(edad));
console.log(typeof(tinesTrabajo));
console.log(typeof(variableNull));

/* var let y const */

/* if (true){
    let numero = 10 //cuando poner var aunque este dentro de una variable aunque este dentro de una variable se puede utilizar despues pero VAR ya no se ocupa mucho debido a que puede usar esa variable
     console.log(numero);
}

for(let i=0; i<=10; i++){
    console.log(i);
} */
